---
interface Props {
  content: string;
  subContent?: string;
}

const { content, subContent } = Astro.props;
---

<h1 class="text-3xl md:text-4xl font-bold tracking-normal leading-none">
  {content}
  <span class="faded">{subContent}</span>
</h1>
